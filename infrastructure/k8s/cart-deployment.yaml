apiVersion: v1
kind: ConfigMap
metadata:
  name: coffee-cart-config
  namespace: cosmic-coffee
data:
  ProductsServiceUrl: "http://coffee-products:4001"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: coffee-cart
  namespace: cosmic-coffee
spec:
  replicas: 1
  selector:
    matchLabels:
      app: coffee-cart
  template:
    metadata:
      labels:
        app: coffee-cart
    spec:
      containers:
      - name: coffee-cart
        image: bpschmitt/cosmic-coffee-cart:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 4003
        envFrom:
        - configMapRef:
            name: coffee-cart-config
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 4003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 4003
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: coffee-cart
  namespace: cosmic-coffee
spec:
  type: ClusterIP
  selector:
    app: coffee-cart
  ports:
  - port: 4003
    targetPort: 4003
    protocol: TCP

