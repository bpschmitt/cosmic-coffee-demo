apiVersion: v1
kind: ConfigMap
metadata:
  name: coffee-orders-config
  namespace: cosmic-coffee
data:
  NODE_ENV: "production"
  DB_HOST: "postgres"
  DB_PORT: "5432"
  DB_NAME: "cosmic_coffee"
  DB_USER: "postgres"
  PRODUCTS_SERVICE_URL: "http://coffee-products:4001"
  FULFILLMENT_SERVICE_URL: "http://coffee-fulfillment:5000"
  ENABLE_N_PLUS_ONE_QUERIES: "false"
---
apiVersion: v1
kind: Secret
metadata:
  name: coffee-orders-secret
  namespace: cosmic-coffee
type: Opaque
stringData:
  DB_PASSWORD: "postgres"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: coffee-orders
  namespace: cosmic-coffee
spec:
  replicas: 1
  selector:
    matchLabels:
      app: coffee-orders
  template:
    metadata:
      labels:
        app: coffee-orders
    spec:
      containers:
      - name: coffee-orders
        image: bpschmitt/cosmic-coffee-orders:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 4000
        envFrom:
        - configMapRef:
            name: coffee-orders-config
        - secretRef:
            name: coffee-orders-secret
        - secretRef:
            name: postgres-secret
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 4000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 4000
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: coffee-orders
  namespace: cosmic-coffee
spec:
  type: ClusterIP
  selector:
    app: coffee-orders
  ports:
  - port: 4000
    targetPort: 4000
    protocol: TCP

