apiVersion: apps/v1
kind: Deployment
metadata:
  name: coffee-payment
  namespace: cosmic-coffee
spec:
  replicas: 1
  selector:
    matchLabels:
      app: coffee-payment
  template:
    metadata:
      labels:
        app: coffee-payment
    spec:
      containers:
      - name: coffee-payment
        image: bpschmitt/cosmic-coffee-payment:v1.0.0
        imagePullPolicy: Always
        env:
        - name: PAYMENT_SLOWDOWN_ENABLED
          value: "false"
        - name: PAYMENT_FAILURE_RATE
          value: "0.0"
        ports:
        - containerPort: 4002
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 4002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 4002
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: coffee-payment
  namespace: cosmic-coffee
spec:
  type: ClusterIP
  selector:
    app: coffee-payment
  ports:
  - port: 4002
    targetPort: 4002
    protocol: TCP

