apiVersion: v1
kind: ConfigMap
metadata:
  name: coffee-products-config
  namespace: cosmic-coffee
data:
  SPRING_PROFILES_ACTIVE: "production"
  DB_HOST: "postgres"
  DB_PORT: "5432"
  DB_NAME: "cosmic_coffee"
  DB_USER: "postgres"
---
apiVersion: v1
kind: Secret
metadata:
  name: coffee-products-secret
  namespace: cosmic-coffee
type: Opaque
stringData:
  DB_PASSWORD: "postgres"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: coffee-products
  namespace: cosmic-coffee
spec:
  replicas: 1
  selector:
    matchLabels:
      app: coffee-products
  template:
    metadata:
      labels:
        app: coffee-products
    spec:
      containers:
      - name: coffee-products
        image: bpschmitt/cosmic-coffee-products:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 4001
        envFrom:
        - configMapRef:
            name: coffee-products-config
        - secretRef:
            name: coffee-products-secret
        - secretRef:
            name: postgres-secret
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: coffee-products
  namespace: cosmic-coffee
spec:
  type: ClusterIP
  selector:
    app: coffee-products
  ports:
  - port: 4001
    targetPort: 4001
    protocol: TCP

