apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: cosmic-coffee
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # Optionally bind to a specific PV by name
  # volumeName: postgres-pv
  # For dynamic provisioning, use a storageClassName instead

